// Landmarks the HUD reads. Defined in shared code so the client prompt and the
// server's proximity check can never disagree.
export { BENCH_RADIUS, CONTEST_SIGN, SHOP_DOOR, SORTING_CRATES, THREADS_DOOR, withinLandmark } from '../../packages/shared/layout';
